# Production-shaped local stack: PostgreSQL, the Quarkus backend and the nginx-served
# frontend, with no identity provider. Sign-in is off unless you supply Google credentials,
# so this is the stack for checking the deployment shape, not for working on the app --
# `./mvnw quarkus:dev` is far better for that. See doc/local-development.md.
#
# The project name is pinned because Compose otherwise derives it from this file's directory,
# which would rename the postgres volume to docker_postgres-data and orphan the existing one.
name: ai-assisted-todolist

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: todolist
      POSTGRES_USER: todolist
      POSTGRES_PASSWORD: todolist
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  backend:
    # Built by Jib, not by a Dockerfile: `cd backend && ./mvnw package
    # -Dquarkus.container-image.build=true` produces this image locally. There is no
    # Dockerfile under backend/ to build from -- the ones Quarkus generates in
    # src/main/docker are JDK 17 based and cannot run this Java 25 code.
    image: ${BACKEND_IMAGE:-quay.io/ghilling/todo-backend:latest}
    environment:
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://postgres:5432/todolist
      QUARKUS_DATASOURCE_USERNAME: todolist
      QUARKUS_DATASOURCE_PASSWORD: todolist
      FRONTEND_ORIGINS: http://localhost:3000
      QUARKUS_OIDC_ENABLED: ${QUARKUS_OIDC_ENABLED:-false}
      TODO_AUTH_ENABLED: ${TODO_AUTH_ENABLED:-false}
      TODO_OIDC_GOOGLE_CLIENT_ID: ${TODO_OIDC_GOOGLE_CLIENT_ID:-}
      TODO_OIDC_GOOGLE_CLIENT_SECRET: ${TODO_OIDC_GOOGLE_CLIENT_SECRET:-}
      TODO_AUTH_POST_LOGIN_REDIRECT_URI: ${TODO_AUTH_POST_LOGIN_REDIRECT_URI:-http://localhost:3000/}
      TODO_OIDC_COOKIE_SECURE: ${TODO_OIDC_COOKIE_SECURE:-false}
    depends_on:
      - postgres
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ../frontend
      dockerfile: docker/Dockerfile
    depends_on:
      - backend
    ports:
      - "3000:80"

volumes:
  postgres-data:
